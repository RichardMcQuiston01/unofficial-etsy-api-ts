/**
 * @richardmcquiston01/etsy-api — public entry point.
 *
 * Scaffolding only for now (ROADMAP.md Stage 1). The client facade,
 * resource modules, auth, and generated types land in later stages per
 * docs/ARCHITECTURE.md.
 */
export {};
